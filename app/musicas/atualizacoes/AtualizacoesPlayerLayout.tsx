"use client";

export function AtualizacoesPlayerLayout({ children }: { children: React.ReactNode }) {
  // Usa o VipMusicPlayerProvider do MusicasAuthLayout (mini player VIP).
  return <>{children}</>;
}
