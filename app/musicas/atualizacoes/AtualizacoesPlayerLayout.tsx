"use client";

export function AtualizacoesPlayerLayout({ children }: { children: React.ReactNode }) {
  // Usa o VipMusicPlayerProvider do MusicasAuthLayout (prévia + mini player).
  return <>{children}</>;
}
